import styled from "styled-components";

const LayoutWrapper = styled.div`
  display: flex;
  margin: 1rem;
  flex-direction: column;
`;

const RowWrapper = styled.div`
  display: flex;
`;

export { LayoutWrapper, RowWrapper };
